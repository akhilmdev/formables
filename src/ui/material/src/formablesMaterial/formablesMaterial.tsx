import React from 'React';
import { FormablesMaterialInterface } from './formablesMaterial.interface';

export const formablesMaterial = (props: FormablesMaterialInterface) => {
    return <div>this is test</div>
}